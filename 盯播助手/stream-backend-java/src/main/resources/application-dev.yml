# 开发环境配置
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/stream_monitor?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: root123
  redis:
    host: localhost
    port: 6379
    password:
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

logging:
  level:
    com.streammonitor: debug
    org.springframework.web: debug
    com.baomidou.mybatisplus: trace
  file:
    name: logs/stream-monitor-dev.log

app:
  config:
    wechat:
      app-id: wx1234567890abcdef
      app-secret: your-development-app-secret
    ai:
      api-key: your-development-ai-key
    crawler:
      interval: 10 # 开发环境更频繁的检查